# 🤖 ROS2 소스 코드 및 알고리즘 설계

## 1. 디렉토리 개요
- 본 `src` 폴더는 오라클 클라우드 서버(OCI)와 로컬 디바이스(Gram) 간의 분산 통신 및 로봇 제어를 수행하기 위한 ROS2 패키지와 노드 소스 코드를 포함합니다.

## 2. 기술적 선택 이유 (Rationale)
- **분산 통신 아키텍처 (ROS_DOMAIN_ID)**: 동일한 가상 네트워크(Tailscale) 상에서 클라우드의 무거운 연산 노드와 로컬의 UI/입력 제어 노드가 데이터를 원활하게 주고받을 수 있도록 시스템을 분리했습니다.
- **개발 언어**: 알고리즘 검증과 빠른 프로토타이핑을 위해 통신 및 제어 노드는 **Python 3**로 작성하며, 향후 시뮬레이터 내부의 물리 연산 등 실시간 처리가 중요한 부분은 **C++**로 최적화할 계획입니다.

"""
@file: remote_teleop_node.py
@brief: 로컬 환경(Gram)에서 클라우드(OCI)의 시뮬레이션 로봇을 제어하기 위한 원격 조향 노드

[기술적 선택 이유 및 설계 의도 (Rationale)]
1. 왜 이 노드를 클라우드가 아닌 로컬에서 실행하는가?
   - 클라우드 서버에는 물리적인 키보드나 조이스틱 등 입력 장치가 연결되어 있지 않습니다.
   - 따라서 로컬 노트북에서 사용자 입력을 받아 제어 메시지(Twist)로 변환한 뒤, 
     Tailscale VPN망을 통해 클라우드 서버의 제어 토픽으로 전송하는 분산 처리 방식을 채택했습니다.
     
2. 타이머(Timer) 기반이 아닌 이벤트 기반 발행을 선택한 이유
   - 주기적으로(예: 10Hz) 정지(0.0) 값을 계속 보내는 대신, 사용자의 입력 상태가 변할 때만
     메시지를 발행하여 네트워크 대역폭 낭비와 클라우드 서버의 연산 부하를 최소화했습니다.
"""

# import rclpy
# ... (이하 실제 코드 작성)